import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";

const eslintConfig = defineConfig([
  ...nextVitals,
  {
    rules: {
      // Fetch-on-mount and localStorage-hydration effects legitimately set
      // state synchronously (loading flags, hydrating from localStorage,
      // bootstrapping auth) — downgraded instead of restructured to avoid
      // SSR/client hydration mismatches those rewrites would introduce.
      "react-hooks/set-state-in-effect": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
